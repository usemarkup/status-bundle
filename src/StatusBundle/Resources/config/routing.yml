# This is an example only really. I would personally not have a /ping endpoint in your application
markup_status_group:
    path: /status/{group}
    defaults: { _controller: MarkupStatusBundle:Status:index, group: ping }
    methods: [GET, HEAD]
